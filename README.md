# VerveSearch Socializer

A PHP Class that generates the  URLs for Facebook, LinkedIn, Twitter and GPlus.

## Usage example

$obj = new Socializer();
$twitterUrl = $obj->generateFacebookUrl('http://www.vervesearch.com/');
